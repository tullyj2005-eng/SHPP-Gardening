name: shpp_gardening
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  audioplayers: ^6.0.0
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  firebase_storage: ^12.0.0


  cupertino_icons: ^1.0.8
  
  # Core Firebase
  firebase_core: ^3.1.1       # Stable version for your current setup
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  
  # Additional Features
  webview_flutter: ^4.13.1
  firebase_functions: ^0.0.1-dev

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/Thyme.png
    - assets/Leaf (1).png
    - assets/audio/soundtrack.wav




  